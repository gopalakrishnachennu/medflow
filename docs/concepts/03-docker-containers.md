# Docker Containers

Each MedFlow service is packaged as an image. Learn Dockerfiles, layers, build cache, image tags, non-root users, environment variables, health checks, and vulnerability scanning.


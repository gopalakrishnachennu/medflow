global:
  scrape_interval: 15s

rule_files:
  - alert-rules.yml
  - recording-rules.yml

scrape_configs:
  - job_name: medflow-auth-service
    metrics_path: /metrics
    static_configs:
      - targets: ["auth-service:8000"]

